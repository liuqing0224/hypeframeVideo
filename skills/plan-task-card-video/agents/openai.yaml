interface:
  display_name: "Plan Task Card Video"
  short_description: "Plan scripts, shots, and blocking from task cards"
  default_prompt: "Use $plan-task-card-video to compile this task card into a HyperFrames script, storyboard, and per-shot blocking plan."
