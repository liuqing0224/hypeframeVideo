interface:
  display_name: "Compose HyperFrames Video"
  short_description: "Compose story-specific manga motion and endings"
  default_prompt: "Use $compose-hyperframes-video to compile this timed production with its director profile, signed shots, and unique ending motif."
