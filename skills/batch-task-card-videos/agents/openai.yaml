interface:
  display_name: "Batch Task Card Videos"
  short_description: "Orchestrate distinct story-directed video batches"
  default_prompt: "Use $batch-task-card-videos to create a batch of story-specific HyperFrames videos with distinct shot signatures and endings."
